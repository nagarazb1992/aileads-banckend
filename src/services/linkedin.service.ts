export async function sendLinkedInMessage(profileUrl: string, message: string) {
  // Recommended:
  // - Phantombuster
  // - TexAu
  // - Waalaxy
  // - Browser extension (user-triggered)

  console.log(`Send LinkedIn message to ${profileUrl}`);
}
