# name: Deploy to AWS EC2

# on:
#   push:
#     branches:
#       - main

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20

#       - name: Install dependencies
#         run: npm ci

#       # No build script present, skipping build step

#       - name: Set up SSH key
#         uses: webfactory/ssh-agent@v0.9.0
#         with:
#           ssh-private-key: ${{ secrets.EC2_SSH_KEY }}

#       - name: Deploy to EC2 and start backend
#         run: |
#           ssh -o StrictHostKeyChecking=no ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} '
#             git clone https://github.com/yourrepo/aileads-backend.git || (cd aileads-backend && git pull origin main)
#             cd aileads-backend
#             sudo npm install -g pm2
#             npm install
#             pm2 delete aileads-backend || true
#             pm2 start npm --name "aileads-backend" -- run dev
#             pm2 save
#           '
#         env:
#           EC2_HOST: ${{ secrets.EC2_HOST }}
#           EC2_USER: ${{ secrets.EC2_USER }}
#           EC2_SSH_KEY: ${{ secrets.EC2_SSH_KEY }}